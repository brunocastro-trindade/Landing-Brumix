/// <reference types="vite/client" />

// Defined inline in index.html (Google Ads event snippet for the "Contato"
// conversion) — not an ES module, so it's declared here as a global.
declare function gtag_report_conversion(url?: string): boolean;
